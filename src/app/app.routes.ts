import { Routes } from '@angular/router';
import { ChatRoomComponent } from './chat/components/chat-room/chat-room.component';

export const routes: Routes = [{ path: '', component: ChatRoomComponent }];
